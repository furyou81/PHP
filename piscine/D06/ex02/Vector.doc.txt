
<- Vector ----------------------------------------------------------------------
The Vector class handles vector.

An instance can be contructed from orig and dest vertex:
new Vector( array( 'orig' => $vtxO, 'dest' => $vtxX ) );
orig is optional and set to (0, 0, 0) by default
Any other use is undefined behaviour.
---------------------------------------------------------------------- Vector ->
